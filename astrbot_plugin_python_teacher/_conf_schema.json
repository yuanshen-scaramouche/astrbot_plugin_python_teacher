{
  "api_base": {
    "type": "string",
    "description": "OpenAI 兼容 Chat Completions 接口的 Base URL，例如 http://localhost:8000/v1 或 https://api.openai.com/v1",
    "default": "http://localhost:8000/v1"
  },
  "api_key": {
    "type": "string",
    "description": "API Key（如使用 mock_llm_server 可填 mock-key）",
    "default": "mock-key"
  },
  "model": {
    "type": "string",
    "description": "模型名称，例如 gpt-4o-mini / qwen2.5:7b / mock-model",
    "default": "mock-model"
  },
  "system_profile": {
    "type": "string",
    "description": "系统提示词：教学风格与约束（可自行修改）",
    "default": "你是一名资深 Python 教练。你会用循序渐进的方式教学：先目标，再解释，再例子，再练习，再检查答案。回答要简洁、准确、可执行。"
  },
  "max_turns": {
    "type": "int",
    "description": "每次对话中允许的最大追问轮数（用于插件内部工具链限制）",
    "default": 6
  }
}
